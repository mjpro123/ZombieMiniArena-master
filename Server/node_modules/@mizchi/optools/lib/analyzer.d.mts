import ts from "typescript";
export type AnalyzeResult = {
    reserved: string[];
    privates: string[];
};
export type CollectPropertiesOptions = {
    ambient?: boolean;
};
export declare const collectProperties: (root: ts.Node, { ambient: ambientMode }?: CollectPropertiesOptions, debug?: boolean) => AnalyzeResult;
